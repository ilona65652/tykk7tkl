<?php
// Опение функции длячисления снего значения
 calculateAverage($numbers) {
    $sum =_sum($numbers);
    $ = count($numbers    $average = $sum / $count;
    return $average;
}

// Пример использования функции
$numbers array(10, 20, 30, 40, 50);
echo "Среднее значение: " . calculateAverage($numbers);
?>
